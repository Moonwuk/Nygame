# Баг-охота 2026-07-10 — триаж перед плейтестом

8 параллельных агентов-охотников по зонам; каждая находка подтверждалась временным
тестом против реального ядра (не только чтением кода). Ниже — сводный список по
серьёзности. Статус: ✅ исправлено · 🔧 в работе · ⏳ ждёт фикса.

Живая QA (CDP-прогон реального клиента в headless-браузере) дала **ноль
непойманных исключений в обоих локалях** — все находки ниже структурные/логические,
не краши.

## CRITICAL

- **BF-1** ✅ (PR #172) `[srv]` Гонка серверных драйверов: на durable-комнате сырой
  sync `submitAction` драйвера (ИИ/стоячие/Хранитель) мутировал `stateValue`/`seq`
  посреди `commitApply`-persist-await → применённое действие затиралось, seq
  откатывался. Фикс: `MatchRoom.submitServerAction` через mailbox на durable-пути.
- **BF-2** ✅ (PR #174) `[cli]` Один `E_RATE_LIMIT` заклинивал gated-сессию НАВСЕГДА:
  `MultiplayerClient.sendAction` жжёт `clientSeq += 1` на каждый send и не откатывает
  при throttle/drop → строгий курсор гейта отвергает всё дальнейшее `E_OUT_OF_ORDER`.
  Триггер в плейтесте: мультиселект-групповой приказ >20 флотов трипает 20/1000ms.
  Фикс: клиент откатывает `clientSeq` на `E_RATE_LIMIT`/drop; и/или resync на
  `E_OUT_OF_ORDER`. Родственное: flood-guard транспорта ДРОПАЕТ >50/s без реджекта —
  та же потеря seq.
- **BF-3** ✅ (PR #174) `[proto]` `[core]` `fleet.merge` осиротлял `hero.fleetId` → бесплатное
  безлимитное дублирование геройского флагмана (`hero.spawn` не блокируется на stale
  fleetId + нет кулдауна respawn). Родственно — `fleet.split` может забрать hero-стек
  без репойнта. Фикс: репойнтить `hero.fleetId` в merge/split как репойнтятся дивизии;
  и/или `hero.spawn` отвергать при `alive === true`.
- **BF-4** ✅ (PR #173) `[proto]` `fleet.split` дублировал HP-пул повреждённого стека
  (`takeFromStacks` копирует весь `st.hp` на взятый стек, source не уменьшает), а
  `applyDamage` пересчитывает `count = ceil(pool/perShip)` без клампа → бой **плодит
  корабли**. NET-эксплойт-дупликатор. Фикс: делить пул пропорционально + клампить
  `newCount ≤ count`.
- **BF-5** ✅ (PR #173) `[proto]` `mergeStacks`/`takeFromStacks` коалесцируют стеки по `(unit,hp)`
  игнорируя `modules`/`shieldHp` → оплаченный лоадаут размазывается на голые корпуса
  (минтинг статов) или уничтожается; split срезает модули с взятых. Ядровые
  `addUnits`/`findHealthyStack` делают правильно (по `loadoutKey`) — чинить прото-хелперы
  тем же ключом.

## MAJOR

### Бой (core)
- **BF-6** ✅ (PR #175) `fleet.engage` (прото) мимо дипломатии: атака игрока в МИРЕ/пакте/альянсе
  (проверяется только `f.owner===target.owner`). Грифинг в мультиплеере.
- **BF-7** ✅ (PR #175) Двойной наземный штурм одного гарнизона: нет «ground battle already live»
  гварда + `findEnemyFleetAt` пропускает battleId-флоты → два штурма делят defender-ref
  → дружеский огонь + двойной `planet.captured`.
- **BF-8** ✅ (PR #175) Штурм при неразрешённой орбитальной битве (E_ORBIT_CONTESTED побеждён тем
  же battleId-слепым `findEnemyFleetAt`).
- **BF-9** ✅ (PR #175) Нет chain-engage после наземной битвы: враждебные флоты вечно
  сосуществуют на узле (victor-блок только для `phase==='orbital'`).
- **BF-10** ✅ (PR #175) Бомбардир, прикованный к мелэ (`battleId`), продолжает шелл планету, а
  ПВО его не видит (`nearOrbitHostile` пропускает battleId) — скрамбл защитника
  ЗАЩИЩАЕТ бомбардира.

- **BF-10b** ✅ (PR #175) Пат (MAX_COMBAT_ROUNDS, нулевой урон с обеих сторон):
  victor-chain-engage немедленно перезапускал идентичную битву — вечный livelock
  битва/релиз. Теперь пат НЕ ре-энгейджит (пара сосуществует released).

### Ядро / время / детерминизм
- **BF-11** ✅ (PR #177) `applyAction` перепрыгивает due-события (стампит `time` без их запуска),
  следующий `advanceTo` крутит часы НАЗАД и переначисляет прожитый спан (инфляция при
  залипшем catch-up). Фикс: reject `E_TIME_GAP` при `scheduled[0].at < now` или
  clamp `stepTime ≥ committed.time`; хосты — surface `partial`.
- **BF-12** ✅ (PR #177) `schedule(NaN/Infinity)` принимается → бессмертное zombie-событие
  (не dead-letter), ломает `(at,seq)` и JSON-сериализуемость. Фикс: `schedule` —
  `reject('E_BAD_SCHEDULE')` при `!Number.isFinite(at)`.
- **BF-13** ✅ (PR #187) Экономика (и hero/steward/intercept) итерируют `Object.keys` без сорта;
  Postgres JSONB-гибернация пересортировывает ключи → float-сложение в другом порядке
  → реплей/десинк-дивергенция. Фикс: канонизировать ключи на границе стора ИЛИ
  сортировать ключи в модулях (как victory/technology/artillery уже делают).
- **BF-14** ✅ (PR #177) (MINOR→важно) `AdvanceResult.failures` (dead-letter) не читает ни один
  прод-код — коррупция без следа. Фикс: пробросить в `observe`/лог.

### Сеть / туман
- **BF-15** ✅ (PR #178) `eventVisibleTo` режет целые классы событий ВСЕМ, включая владельца
  (`diplomacy.offered/changed`, `technology.researched`, `steward.*`, `market.*`,
  `player.eliminated` — payload-ключи не совпадают с `owner/location/planetId/at/fleetId`).
  Тосты «изучено»/утренний отчёт мертвы в NET. Фикс: матчить `playerId` + парные
  `a/b/from/to/buyer/seller`.
- **BF-16** ✅ (PR #178) Приватный герой течёт сквозь туман: `hero.spawned/respawned` несут `at`
  (узел), `eventVisibleTo` раскрывает событие на опознанном узле → враг получает
  `{owner,heroId,fleetId,at}` для героя, которого проекция прячет. Фикс: `hero.*`
  события owner-only.
- **BF-17** ✅ (PR #188) ИИ-замена без грейс-периода захватывает место человека при рестарте/дропе
  (`humans` пуст после рестарта → первый heartbeat гоняет expand-AI за место другого).
  Фикс: грейс-окно N минут, seed из claimed-мест стора.
- **BF-18** ✅ (PR #185) Радар-сигнатуры не рендерятся в NET: сервер шлёт `signatures[]`, но
  `MultiplayerClient` их не парсит, а прототип считает блипы из `s.fleets` (где
  радар-онли врагов уже нет). Враги «выскакивают» из невидимости в опознанные. Фикс:
  прокинуть `signatures`/`remembered` через `MultiplayerSnapshot`.

### Наземка / герои / H4
- **BF-19** ✅ (PR #181) Замок офицеров H4 — UI-only: `division.officer` живой и в gate-схеме →
  сырое действие цепляет любого офицера на дешёвую дивизию бесплатно / снимает с
  «запертого» премейда. Фикс: удалить хендлер+схему+билдер или отвергать не-премейд.
- **BF-20** ✅ (PR #181) `division.rename` отсутствует в payloadSchemas → на gated-сервере
  (релиз-постура) переименование мёртво. `gateparity.test.ts` не обновлён H4. Фикс:
  добавить схему + оба parity-сэмпла (rename + officer-mobilize).
- **BF-21** ✅ (PR #181) Офицерские премейды мобилизуются БЕЗ HP-бонуса офицера (`makeSide`
  без 3-го арг), но `regenDivision` его учитывает → рождается ниже своего же regen-max.
- **BF-22** ✅ (PR #181) Наземный бой зависит от шага времени: daily-regen применяется раз-в-спан
  посреди боя → coarse vs fine stepping расходятся (ломает реплей/JSONB-паритет).
  Фикс: квантовать regen по тику ИЛИ гасить regen пока идёт `groundBattles[planetId]`.
- **BF-23** ✅ (owner: вариант «убрать из рекламы») Доктрины/синергии дизайнера давали ноль
  боевого эффекта (`formationStats` кормил только UI-превью; бой резолвит из `GROUND_ROSTER`
  +офицер, `formationStats.attack/defense/synergies` не читаются ни боем, ни мобилизацией,
  ни state). Фикс: убрать фантомный `atkMul/defMul` (боевого множителя больше нет);
  синергии остаются как **организационные метки доктрины** (описания без «+X%»), «Синергии
  состава» → «Доктрина состава». Комментарии интерфейса/функции, лгавшие про множитель,
  исправлены. Бой не тронут (проверено ревью: изменение display-only, геймплейно-нейтральное).
  _Хвост (решён владельцем, вариант «сделать честным»):_ агрегатные ⚔/🛡 превью раньше
  считались из `data.units.stats` — таблицы, которую бой не читает, — и расходились с честной
  матрицей «Урон по:». Теперь `attack/defense` = Σ по слотам среднего урона юнита по 4 целям
  из того же `GROUND_ROSTER`, что и бой → сводный рейтинг = среднее тех же 4 баров «Урон по:»
  (тождество Σ_slots·mean_t = mean_t·Σ_slots), grounded в реальном бою. Display-only,
  геймплейно-нейтрально (проверено ревью: consumers-consistency clean).
- **BF-24** ✅ (PR #181) Резервные (не задеплоенные) герои — неуязвимые кастеры: `hero.ability`
  их принимает (scan/аура/annihilate без риска и вне HERO_ACTIVE_CAP). Фикс: гейтить
  `hero.ability`/пассивы/reveal на `alive === true`.

### Флот / эскадрильи
- **BF-25** ✅ (PR #183) Коллизия fleet-id молча удаляет корабли: `seq = keys.length`, delete
  освобождает номер → split→merge→split в тот же мс регенерирует существующий id и
  перезаписывает флот. Фикс: монотонный `fleetSeq` в state (как `battleSeq`).
- **BF-26** ✅ (PR #183) `order.scramble` OFF→ON мгновенно заправляет сухое/перезаряжающееся
  крыло (`freshSortie(max)` всегда), обходя весь sortie/rearm-механизм. Wire-legal →
  NET пропускает кулдаун бесплатно. Фикс: хранить sortie на флоте, не только в patrol.
- **BF-27** ✅ (PR #183) Гарнизон под наземным штурмом эвакуируется (`army.load`/`fleet.launch`
  без battle-lock) → атакующий выигрывает без потерь, защитник уходит от резолва.
  Фикс: отвергать load/launch пока жива битва с `ref{garrison,planetId}`.
- **BF-28** ✅ (PR #183) `fleet.launch` полностью обходит cargo-cap (поднял 20 танков за один
  крузер как `landing`). Фикс: капить `landing` по `Σ cargoCapacity`.
- **BF-29** ✅ (PR #183) Авто-rally срезает оплаченный лоадаут с построенного корабля
  (`unit.built` несёт `modules`, rally-хендлер их теряет) — «Оснащение» = ресурс-сток.
- **BF-30** ✅ (PR #175) Патруль жёг топливо по цели, уже запертой в бою (`fleet.engage` →
  `E_IN_BATTLE`, но `patch{fuel-1}` персистится до/вне исхода). Враг может запереть
  жертвенный флот в радиусе патруля и осушить его навсегда. Фикс: фильтровать
  `g.battleId` из целей (оба драйвера).

### Дипломатия / победа
- **BF-31** ✅ (PR #172) Коалиция = связная компонента (транзитивная) вместо клики:
  цепочка A–B–C при войне A–C побеждала вместе; непричастный третий блокировал победу.
  Фикс: коалиция — клика (все пары взаимно alliance).
- **BF-32** ✅ (PR #182) `diplomacy.declare` не проверяет `status==='active'` актора/цели:
  побеждённые остаются полными дип-акторами, офферы к мёртвым висят вечно (sweep
  только в момент elimination). Фикс: гейт статуса как в espionage.
- **BF-33** ✅ (PR #182) Бот-дипломатия: гвард мёртвого места (`if (!players[bot])`) — dead-code
  (elimination ставит `defeated`, не удаляет запись) → побеждённый бот шлёт войну «из
  могилы» и отвечает на офферы. Фикс: `players[bot]?.status !== 'active'` + sweep
  `approval` на elimination.
- **BF-34** ✅ (PR #192) (MINOR) Нет терминального гейта после конца матча: `applyAction`
  применял действия при `match.status==='ended'` (мир мутирует и персистится
  бесконечно). Фикс: fail-secure гейт в `applyAction` после проверки хендлера —
  `match.status==='ended'` → `E_MATCH_ENDED`. `advanceTo` не тронут: уже начатые
  бои досчитываются планировщиком, гейт режет только новый игроцкий ввод. Будущий
  «круг почёта» / grace-период ослабит именно эту проверку.
- **BF-35** ✅ (PR #179) (MINOR) `netIncome` HUD не учитывает `economy.production`-хуки
  faction/tech → у всех мест (фракция blue +12%) верхняя `+/ч` неверна с 1-й минуты.

## MINOR / хвост (не блокируют плейтест)
- ✅ (PR #186) Разделитель offer-key `>` не забанен в id-схеме (только `|`) — теоретическая
  мисатрибуция оффера.
- ✅ (PR #186) Домино-победа без tie-handling при `dominationPercent ≤ 0.5`.
- ✅ (PR #186) `market.list` не `typeof`-чекает `price`/`amount` (только ungated).
- ✅ (PR #189) ~30 непереведённых msgid'ов в `en.ts` (конструктор дивизий, окно героя,
  дерево скиллов) + H4-заголовки (`data-i18n`) без ключей. Все переведены; reverse-leak'и
  и identity-leak'и проверены и чисты. Гвард `i18n.test.ts` пинит полное покрытие.
- UI (CDP): ✅ (PR #191) рейл-кнопка «Дипло» на коротком вьюпорте уходила под топ-бар
  (тап открывал playercard) — рейл рос вверх без ограничения; кап `#railtools`
  `max-height: calc(100dvh − 120px)` + внутренний скролл держит его ниже топ-бара;
  ✅ (PR #191) Android-back без открытых слоёв молча выходит из матча
  (соло-матч теряется) — теперь сентинел взводится и в «голом» матче, первый back даёт
  хинт, второй в окне 2.5с выходит; ✅ (PR #191) Escape не закрывает окна — привязан к
  closeTopLayer (десктоп-паритет с back), игнорируется при вводе в поля; ✅ «Вход по
  позывному» не спрашивал позывной (стаб = «Новый командир») — теперь раскрывает инлайн-поле,
  входит под введённым позывным и запоминает его (`void.nick`, следующий визит узнаёт тебя);
  отличается от «Новый командир» (тот подсказывает позывной). Аккаунт-бэкенда пока нет —
  локальная личность, не кросс-девайс (accounts-roadmap.md). Проверено CDP end-to-end.
  ✅ Двойной ре-фит офицерских аур на общий кулдаун (designer look) — **разобрано, не баг**:
  аура `rally_beacon` (+8% урона флотам рядом, `combat.damage`/`ownFleetsNear`) определена в
  данных, но в бой НЕ проведена (у хука ноль применений; `heroPassives` читается только в своём
  определении), а массив `passives` героя-инстанса нигде не рендерится и движком не читается
  (только в тестах). Значит двойной грант (стартовый пассив + `void_attunement` + `aegis_matrix`)
  инертен — ни боевого дабл-эффекта, ни дубля в UI. Способности уже дедупятся (`new Set`,
  game.ts). Дедуп пассивов осмыслен ЛИШЬ когда ауру проведут в бой; сейчас правка неисполняемого
  кода была бы спекулятивной, поэтому кода не трогал.

## Порядок исправления
CRIT → MAJOR по зонам (сеть/бой/ядро дают паритет и честность — вперёд), мелкими PR,
каждый с регресс-тестом из репро охотника. UI/локаль-хвост — отдельным проходом.
