# Эскадрильи и авианосцы — технический roadmap

> Палубные **эскадрильи** (фигтеры), запускаемые с кораблей-**авианосцев** и звёздных
> крепостей: бьют в радиусе, тратят топливо и возвращаются на перезарядку, патрулируют
> зону. **Десантный вариант** (Фаза 7) — та же эскадрилья, но при штурме планеты/крепости
> **превращается в дивизию**. Новая механика = **новый модуль ядра + данные**, без правки кернела
> (`CLAUDE.md`). Парные доки: `gdd.md` (§6 корабли/модули, §7 бой), `combat`-модуль
> (орбитальный слой/ПВО/тиры), `modulesystem.md` (шина/хуки), `economy-roadmap.md`
> (модули/оружие как предметы, P2W), `map-roadmap.md` (дистанции, Евклид), `core-roadmap.md`.
> Формат — кирпичики: `id · зона · статус · deps · «Готово, когда»`. Зоны: `[core]` ·
> `[data]` · `[cli]`. Статусы: ✅ реализовано · ⏳ можно брать · 🔒(dep).

---

## Статус реализации

Реализовано в прототипе (модель «вылет юнитом», переиспользует `fleet.split` + `combatModule`):

- ✅ **SQ-0.1** — данные: `fighter_squadron` + `strike_carrier` в `data/units.json` и инлайн-бандле
  прототипа; стат-поля `strikeRange`/`fuel`/`rearmRounds` в схеме.
- ✅ **SQ-1.1** — запуск крыла: носитель отделяет `squadron`-корабли в отдельный быстрый флот
  (кнопка «🛩 Запустить эскадрилью», оба корабля строятся). `squadronTake` — чистый.
- ✅ **SQ-1.2** — удар обычным боем + встроенный counter ПВО (`runOrbital` бьёт `aaDamage` по
  крылу у ближней орбиты); регресс-тест «`squadron`-флот выбивается ПВО».
- ✅ **SQ-2.1** — топливо/перезарядка: чистый счётчик `SortieState` (`spendSortie`/`tickRearm`).
- ✅ **SQ-3.1** — `strikeRange`: евклидов радиус удара (`squadronReaches`), граница включительно.
- ✅ **SQ-4.1** — патруль: детерминированное решение `patrolTarget` (ближайший по id в радиусе,
  пока крыло боеготово); живой драйвер — CC-4 ниже.

Командные цепочки (`docs/`-соседи по геймплею; здесь как потребители машинерии эскадрилий):

- ✅ **CC-4** — дежурный вылет: `scrambleOrder` + драйвер кадра `drivePatrols` — авто-удар по
  опознанной враждебной цели в зоне видимости + радиуса, топливо/перезарядка (single-player).
- ✅ **CC-server** — цепочка приказов повышена в авторитетное durable-состояние (`state.orders`,
  `orderQueueModule`) и ведётся сервером (`serverQueueActions` + `runServerQueues` в netserver),
  так что цепочки идут офлайн в MP.

Дальше по фазам ниже: SQ-1.3 (ремонтные дроны), SQ-2.2 (политика восстановления), SQ-4.2
(рендер зоны патруля), Фазы 5–7 (постройка/крепость, модули/оружие, десантная эскадрилья).

---

## 0. Перевод в дискретную детерминированную модель (read first)

Игрок описывает фигтеры в риалтайм-терминах (летают, топливо, кружат, возвращаются).
Ядро — **дискретное и детерминированное** (почасовые раунды боя, событийное движение,
непрерывный орбитальный слой на `time.advanced`). Перевод обязателен:

| Риалтайм-описание | В ядре |
|---|---|
| «запускаются с кораблей» | авианосец **несёт** эскадрильи в **ОБЩЕМ трюме** (`cargoCapacity`/`cargoSize`, как десант `landing` — **НЕ** отдельный ангар) |
| «летают в радиусе» | **ВЫЛЕТ ЮНИТОМ**: эскадрилья выходит из трюма как короткодальний флот, дерётся обычным боем и возвращается на перезарядку. `strikeRange`/`fuel` = как далеко борт отлетает от носителя (не проекция-удар) |
| «топливо, возвращаются» | счётчик **вылетов/топлива**; по исчерпании — `rearming`-cooldown K раундов на материнском корабле (детерминированно, как кулдауны героев) |
| «авианосец имеет лимит» | `carrierCapacity` (стат) |
| «режим патрулирования, круг» | приказ `patrol(точка, радиус)`; враг, **вошедший в радиус** (на событиях движения + Евклид-чек, как ПВО/`engageFleets`), авто-атакуется; затем перезарядка |
| «настраивать модули/оружие» | лоадаут через систему модулей (GDD §6.1) + экономику (модули из аукциона/крафта/лутбокса) |
| «строить на планетах/крепостях» | правило construction-модуля: авианосцы — на верфи; эскадрильи базируются на планете/звёздной крепости |

**Готовый counter — ПВО.** `aaDamage` (юнит `orbital_aa`, гарнизон) уже бьёт по флотам
у ближней орбиты — он же сбивает эскадрильи. Камень-ножницы-бумага: фигтеры ↔ ПВО.

**Модель удара — ЗАФИКСИРОВАНО: «вылет юнитом».** Эскадрилья **выходит из трюма как
отдельный короткодальний флот** в узле носителя, летит к цели и дерётся **обычным
движением + боем** (переиспользуем `movement`/`combat`), затем возвращается к носителю на
перезарядку (**куда** — по политике восстановления, SQ-2.2). **НЕ** проекция-удар орбитальным слоем. Плюсы: переиспользует готовый бой и
ПВО — эскадрилья реальный юнит, `aaDamage` бьёт по ней штатно; `strikeRange`/`fuel`
ограничивают, как далеко борт отлетает от носителя и возвращается. (Патруль/радиус —
поверх этого: вылет по событию «враг вошёл в радиус».)

---

## Фаза 0 · Модель данных `[data][core]`

### SQ-0.1 · Эскадрилья как юнит + ангар авианосца `[data][core]` 🔒(—) — M
**Цель:** эскадрильи и вместимость — данными, расширяемо.
**Подзадачи:** юнит с трейтом `squadron` (домен `space`); едет в **ОБЩЕМ трюме**
(`cargoCapacity`/`cargoSize`, как десант — **НЕ** отдельный ангар); статы `strikeRange`,
`fuel` (вылеты до перезарядки), `rearmRounds`. Полиморфный груз трюма: **дивизия |
эскадрилья | десант**. zod-валидация; пример эскадрильи + авианосца в `data/units.json`.
**Готово, когда:** новая эскадрилья добавляется данными; носитель несёт груз ≤ `cargoCapacity` по `cargoSize`; кривая карта отвергается.

### SQ-0.2 · Слоты лоадаута (под Фазу 6) `[data]` 🔒(SQ-0.1) — S
**Подзадачи:** поля слотов модулей/оружия на эскадрилье (пока декларативно), tag `horizontal|vertical` для §5.2/экономики.
**Готово, когда:** лоадаут парсится; модификаторы пока не применяются (Фаза 6).

## Фаза 1 · Запуск и участие в бою `[core]`

### SQ-1.1 · `fleet.launch` — выпустить эскадрилью `[core]` 🔒(SQ-0.1) — M
**Подзадачи:** выпуск = эскадрилья выходит из трюма носителя **отдельным короткодальним флотом** в узле носителя (вылет юнитом); дальше — обычное движение/бой; server-authority/fail-secure коды.
**Готово, когда:** носитель выпускает эскадрилью как флот; пустой трюм → отказ; тест.

### SQ-1.2 · Удар эскадрилий + ПВО-counter `[core]` 🔒(SQ-1.1) — M
**Подзадачи:** вылетевшая эскадрилья дерётся **обычным боем** (`combat`) у цели; ПВО (`aaDamage`) бьёт по ней как по любому флоту у орбиты. (Реализует зафиксированную модель «вылет юнитом».)
**Готово, когда:** эскадрилья наносит урон цели в радиусе; гарнизонное ПВО её прорежает; детерминированный лог.

### SQ-1.3 · Ремонтные дроны — саппорт-вариант (хил корпуса) `[core][data]` 🔒(SQ-1.2, shields SH-0.2) — L
**Цель:** саппорт-эскадрилья (идея №3) — лечит **корпус** флотов в радиусе; **единственный in-combat ремонт** (см. `shields-roadmap.md` SH-2.2 и матрицу §1).
**Подзадачи:**
- Та же машинерия эскадрильи (ангар SQ-0.1, запуск SQ-1.1, топливо SQ-2.1, радиус SQ-3.1, патруль SQ-4.1), но **действие = хил корпуса**, не удар: «обратная бомбардировка» — восстанавливает `hp` (корпус) флотам в радиусе по `time.advanced` (та же модель, что удар SQ-1.2, со знаком минус).
- Лечит **свои и союзные** флоты (координация — стыкуется с корпорациями/AvA); **корпус, не щит** (щит регенит сам вне боя, `shields` §1).
- **Работают и в активном бою** — единственный источник восстановления в бою; ПВО (`aaDamage`) — counter (дроны уязвимы как эскадрилья, их прикрывают/выбивают).
- Патруль-режим лечит флоты в зоне, не атакует.
**Готово, когда:** дроны восстанавливают корпус своим/союзным в радиусе (вкл. активный бой); щит не трогают; ПВО их сбивает; тест.

## Фаза 2 · Топливо и перезарядка `[core]`

### SQ-2.1 · Счётчик вылетов + rearm-cooldown `[core]` 🔒(SQ-1.2) — M
**Подзадачи:** `fuel` убывает за вылет/раунд; по нулю — `rearming` на `rearmRounds` (недоступна, «вернулась на корабль»); затем восстановление. Чистый детерминированный счётчик в стейте (как `heroes.cooldowns`).
**Готово, когда:** эскадрилья после N вылетов уходит на перезарядку и возвращается; воспроизводимо.

### SQ-2.2 · Политика восстановления (ближайшая база | origin) `[core][cli]` 🔒(SQ-2.1) — M
**Цель:** игрок выбирает, куда борт уходит на перезарядку (тоггл, как fire-mode у артиллерии).
**Подзадачи:** поле/приказ `recover: 'nearest' | 'origin'` на эскадрилье. `nearest` → ближайшая
**своя** точка восстановления: носитель со свободным трюмом **|** планета **|** звёздная крепость.
`origin` → узел/носитель, откуда вылетел. По нулю топлива борт обычным `movement` летит туда и
встаёт на `rearming`. **Фолбэки:** `origin` уничтожен/трюм полон → авто-`nearest`; нет
достижимой точки в дальности → борт «на честном слове» (застрял/потерян — тюнится). Выбор
детерминированный (стабильный тай-брейк по id при равной дистанции).
**Готово, когда:** борт восстанавливается по выбранной политике; фолбэк `origin`→`nearest` работает; тест.

## Фаза 3 · Радиус `[core]`

### SQ-3.1 · `strikeRange` — проекция в радиусе `[core]` 🔒(SQ-1.2) — M
**Подзадачи:** выбор целей в Евклидовом радиусе от авианосца (как `radarRange`/`map-roadmap`); удар по узлам в радиусе, не только в своём. Авианосец вне радиуса цели — не бьёт.
**Готово, когда:** удар достаёт цель в радиусе и не достаёт вне его; тест на границе.

## Фаза 4 · Патруль `[core][cli]`

### SQ-4.1 · `fleet.patrol(точка, радиус)` `[core]` 🔒(SQ-3.1, SQ-2.1) — L
**Подзадачи:** приказ патруля; враг, входящий в радиус (на `fleet.arrived`/`fleet.transit` + Евклид-чек, как `engageFleets`/ПВО) → авто-удар эскадрильей → перезарядка → патруль продолжается. Без живого игрока в моменте (детерминированно).
**Готово, когда:** вход врага в зону патруля авто-вызывает удар; после перезарядки патруль активен снова.

### SQ-4.2 · Рендер зоны патруля `[cli]` 🔒(SQ-4.1, CP-рендер) — S
**Подзадачи:** круг патруля + статус эскадрилий (в вылете / на перезарядке) в «Голопульте».
**Готово, когда:** зона и состояние видны на карте.

## Фаза 5 · Постройка (где можно) `[core][data]`

### SQ-5.1 · Ограничение постройки + звёздная крепость `[core][data]` 🔒(SQ-0.1) — M
**Подзадачи:** правило construction-модуля — авианосцы только на верфи (`shipyard`); эскадрильи базируются на планете/звёздной крепости. **Звёздная крепость** = апгрейд `fort` или новый билдинг, способный базировать/запускать эскадрильи (статичный «авианосец»); лимит базирования стройкой.
**Готово, когда:** эскадрилью/авианосец нельзя построить вне планеты/верфи/крепости (отказ + тест).

## Фаза 6 · Конфигурация (модули/оружие) `[core][data]`

### SQ-6.1 · Смена модулей/оружия эскадрильи `[core][data]` 🔒(SQ-0.2, economy) — L
**Подзадачи:** лоадаут через систему модулей (GDD §6.1): модификаторы статов через хуки (`computeDamage`/`computeHP`). Источник модулей — экономика (`economy-roadmap.md`: аукцион/крафт/лутбокс, P2W); силовые модули = вертикальная ветвь (премиум-неускоряемая ось, см. `account-level.md`).
**Готово, когда:** смена модуля меняет статы эскадрильи через хук; данными, без правки логики.

## Фаза 7 · Десантная эскадрилья (конверсия в дивизию) `[proto][core][data]`

> Та же машинерия «носитель + вылет юнитом», но борт наземный: **десантная эскадрилья**
> при штурме **превращается в дивизию**. Носитель `dropship` (большой трюм `cargoCapacity 8`,
> мало пушек `attack 2`) — **уже готов** в прототипе.

### SQ-7.1 · Десантная эскадрилья → дивизия `[core][proto]` 🔒(SQ-1.1) — L
**Цель:** наземный десант той же машинерией — `squadron`-тип, который при штурме конвертируется в дивизию.
**Подзадачи:** юнит `landing_squadron` (трейт `squadron`); при **постройке** выбираешь целевую
дивизию (шаблон); дороже обычной дивизии (+~25% — за доставку/конверсию); `cargoSize 2`. Вылет
юнитом (SQ-1.1); при ударе по враждебном/ничейном **захватываемом** секторе (планета — орбита
чиста; Void-станция `orbit:false` — сразу) **конвертируется в выбранную дивизию**, та воюет на
земле (reuse `runGroundCombat`/`captureGround`). **HP дивизии ∝ доле HP эскадрильи** на момент
конверсии (сбили на подлёте → слабее десант).
**Готово, когда:** десантная эскадрилья высаживается и превращается в выбранную дивизию; HP перенесено; тест.

### SQ-7.2 · Десант бьётся с гарнизоном `[core]` 🔒(SQ-7.1) — M
**Цель:** снять «шов» — гарнизон мира защищает его в наземном бою против десанта.
**Подзадачи:** убрать ранний выход `captureGround` при непустом `garrison`; гарнизон
(`marine`/`orbital_aa`) — защитная сторона наземного боя; захват только после зачистки.
_(Развилка: гарнизон-ростер ≠ дивизион-ростер `GROUND_ROSTER` — нужен мост / единая модель.)_
**Готово, когда:** десант бьётся с гарнизоном; захват только после зачистки; тест.

---

## Открытые вопросы

```
✔ Модель удара: ЗАФИКСИРОВАНО «вылет юнитом» (эскадрилья = короткодальний флот, обычный `movement`/`combat`); НЕ орбитальный-слой.
✔ Трюм: ЗАФИКСИРОВАНО ОБЩИЙ трюм (`cargoCapacity`/`cargoSize`), без отдельного `carrierCapacity`-ангара.
□ Эскадрилья: трейт `squadron` на space-юните (рекоменд.) vs новый домен `fighter`.
□ Топливо: счётчик вылетов (рекоменд.) vs часы; сколько rearmRounds.
□ Точное правило патруль-радиуса: одна цель/раунд vs все в радиусе; приоритет целей.
□ Звёздная крепость: апгрейд `fort` vs новый билдинг; лимит базирования эскадрилий.
□ Эскадрильи в туман войны: как видны разведке (своя `signature`?).
□ Стэкаются ли эскадрильи как обычные юниты (count/hp-пул) или поштучно.
```

## Инварианты (нарушение = баг)

- **Детерминизм:** радиус = Евклид (как радар), топливо = счётчик, патруль = событие
  «вход в радиус»; **никакого континуального полёта** и `Date.now`/`Math.random` в ядре.
- **Только модуль + данные:** новый модуль `squadron`/`carrier` + JSON; **кернел не трогаем**.
- **ПВО — встроенный counter** (`aaDamage` уже есть); фигтеры не должны быть без контр-игры.
- **Server-authority + fail-secure:** `launch`/`patrol` — интенты со стабильными кодами.

---

## Последовательность

`SQ-0.1` (данные) → `SQ-1.1/1.2` (запуск + удар + ПВО) → `SQ-2.1` (топливо) → `SQ-3.1`
(радиус) → `SQ-4.*` (патруль) → `SQ-5.1` (постройка) → `SQ-6.1` (лоадаут, после экономики).
Десант: `SQ-7.*` (десантная эскадрилья → дивизия + бой с гарнизоном) — на машинерии SQ-1.1.
Бой-модель удара (SQ-1.2) — критическое решение, фиксируется до Фазы 2+.
