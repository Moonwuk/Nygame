{
  "name": "@void/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "PWA-first web client (Stage 4): framework-agnostic view-models + a Vite app shell consuming @void/shared-core.",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=./",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@void/action-layer": "workspace:*",
    "@void/shared-core": "workspace:*"
  },
  "devDependencies": {
    "vite": "^8.1.3"
  }
}
