# Шаблон плейтеста (2 человека) — чеклист

Заполняется **после каждого живого теста** на двоих. Цель — чтобы у каждого теста
были и машинные данные (JSONL-лог сервера), и человеческие наблюдения (баги, что
путало, фан). Это брик **M0** из `docs/metrics-roadmap.md`; как поднять сервер —
`docs/multiplayer.md`.

Копируй блок ниже в новый файл (например, `playtest-logs/2026-06-26-notes.md`) и
заполняй. Машинный лог лежит рядом: `playtest-logs/proto-<timestamp>.jsonl`
(пишется сам), сводка печатается в терминал сервера по `Ctrl-C`.

---

## Метаданные прогона

- **Дата / время:**
- **Билд:** (git-хеш `git rev-parse --short HEAD`; размер из `pnpm prototype`)
- **Игроки:** p1 (Azure / зелёный) = … · p2 (Crimson / красный) = …
- **Как поднят сервер:** ☐ LAN (`pnpm host`) ☐ туннель (cloudflared) ☐ деплой (ссылка)
- **Устройства / сеть:** (ОС, браузер или APK; Wi-Fi / мобильный интернет; примерное расстояние)
- **Файл лога:** `playtest-logs/proto-________.jsonl`

## A · Связь (из оверлея + лога сервера)

Оверлей в углу клиента: `NN FPS · NN ms · sync ✓` (при рассинхроне — `desync ✗ K`).

- ☐ Оба игрока подключились с первой попытки. Если нет — что мешало:
- **Time-to-connect** (Connect → первый кадр мира), оценка: p1 ≈ … c · p2 ≈ … c (цель < 3 c)
- **Авто-старт (SES-2.1, лобби нет):** войдя, игрок сразу попал в уже идущий мир (часы шли,
  без Day 1-заморозки)? ☐ да ☐ нет. Свободное место было видно в браузере матчей? ☐ да ☐ нет
- **RTT** из оверлея (типичное): p1 ≈ … мс · p2 ≈ … мс (цель p95 < 150 мс)
- **Дисконнекты за сессию:** сколько раз сокет рвался: … (цель < 1)
- **Reconnect:** после обрыва вход восстановился? ☐ да ☐ нет ☐ не проверяли

## B · Корректность / десинк

- **Индикатор desync** хоть раз показал `✗`? ☐ нет (хорошо) ☐ да →
  на каком действии / в какой момент:
- **Отклонённые действия** (`✖ …` в логе дёржавок клиента): какие и когда:
- Картинка у обоих совпадала (одни и те же флоты/планеты/бои)? ☐ да ☐ были расхождения →

## C · Производительность (клиент)

- **FPS** из оверлея: покой ≈ … · пан/зум ≈ … (цель ≥ 50, на GPU 60)
- Заметные просадки/лаги — где (бой, много флотов, зум)?
- Память/перегрев за матч (телефон грелся, вкладка тормозила)? ☐ нет ☐ да →

## D · Баги и краши

- **Краши / белый экран / зависания:** (что делали перед этим — шаги к воспроизведению)
- **Ошибки в консоли** (DevTools → Console, если открывали):
- Прочие визуальные глюки (радар, провинции, флоты на линиях):

## E · UX — что путало

- Что было непонятно с первого раза (управление, заказы, постройки, бой):
- Чего не хватило (подсказка, фидбэк на действие, индикатор):
- Что, наоборот, зашло сразу:

## F · Фан и итог

- **Фан (1–5):** p1 = … · p2 = …
- Хотелось бы доиграть до конца? ☐ да ☐ нет ☐ скучно стало на …
- Топ-1 что чинить в следующий раз:
- Свободные заметки:

---

## Машинные данные (приложить к заметкам)

1. **Сводка сервера** — скопировать из терминала блок `── playtest summary ──`
   (joins/leaves/actions/ok/rejects/by type/by reject/match end).
2. **JSONL-лог** — `playtest-logs/proto-<timestamp>.jsonl` (по строке на событие:
   `join` / `leave` / `action` / `end`; `lobby` оставлен для паритета транспорта, но при авто-старте SES-2.1 не эмитится). Быстрый разбор, например:
   ```bash
   # сколько действий каждого типа и сколько реджектов
   grep '"kind":"action"' playtest-logs/proto-*.jsonl | wc -l
   grep '"ok":false' playtest-logs/proto-*.jsonl
   ```
3. Если ловили desync — сохранить кадр/скрин оверлея с `✗` и время.
