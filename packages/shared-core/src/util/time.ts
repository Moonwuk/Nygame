/** Milliseconds in one hour — the base unit for all real-time durations. */
export const MS_PER_HOUR = 3_600_000;
/** Milliseconds in one day — used for daily-rate computations (upkeep). */
export const MS_PER_DAY = 86_400_000;
