{
  "name": "@void/client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "React Native/mobile client package (Stage 4) with a minimal multiplayer transport adapter.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@void/shared-core": "workspace:*"
  }
}
